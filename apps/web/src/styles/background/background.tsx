import * as s from "./background.module.css";

export const background = {
  base: s.base as string,
  surface: s.surface as string,
  overlay: s.overlay as string,
  baseBlur: s.baseBlur as string,
};
